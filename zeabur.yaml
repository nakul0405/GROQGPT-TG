project:
  name: telegram-groq-bot

services:
  - name: bot
    build:
      type: python
      entry: main.py
    env:
      - TELEGRAM_TOKEN
      - GROQ_API_KEY
    autoDeploy: true
# Created by Nakul Rathod
